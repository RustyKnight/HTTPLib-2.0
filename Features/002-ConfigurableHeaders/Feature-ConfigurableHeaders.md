# HTTP Library

Add support for the user to supply default header values via the initialiser, which are applied automatically which each request, in addition to any headers passed to the method functions.